# Вокруг света за 80 дней

Android-приложение, в котором реальные шаги продвигают путешественника по кругосветному маршруту, открывают дневник и штампы. Визуальное направление — иллюстрированный атлас XIX века. Fantasy Hike служит концептуальным референсом; код, оформление и контент создаём свои.

## Первый результат

Лондон → Суэц: реальные шаги, движение по маршруту, первое событие дневника и сохранение прогресса после перезапуска. Без аккаунта, обязательного сервера и постоянного GPS.

Сейчас создан стартовый экран на Compose. Учёт шагов, игровой расчёт и сохранение ещё не реализованы. Health Connect и необходимость отдельного источника шагов предстоит проверить на реальном телефоне.

## Сборка и запуск

Требуются JDK 17 или 21, Android SDK Platform 36 и Build Tools 35.0.0. Установите их через Android Studio/SDK Manager. Укажите путь к SDK в `local.properties` (`sdk.dir=/абсолютный/путь/к/sdk`) либо задайте `ANDROID_HOME`.

```sh
./gradlew assembleDebug lintDebug
# После подключения устройства с разрешённой USB-отладкой:
./gradlew installDebug
```

Откройте приложение «Вокруг света за 80 дней» на устройстве. APK: `app/build/outputs/apk/debug/app-debug.apk`.

GitHub Actions собирает APK и запускает Android Lint на push и pull request; APK доступен в артефактах запуска `Android`. Сборка не заменяет проверку запуска и настоящих шагов на телефоне.

## Техническая основа

- Kotlin 2.2.21, Compose Compiler той же версии, Compose BOM 2025.10.01.
- Android Gradle Plugin 8.13.2, Gradle Wrapper 8.13.
- Android 9+ (minSdk 28), compileSdk/targetSdk 36; applicationId пока предварительный.
- Room и Health Connect запланированы, зависимости пока не добавлены.

Выбор AGP/Gradle основан на [таблице совместимости Android](https://developer.android.com/build/releases/agp-8-13-0-release-notes), подключение компилятора — на [инструкции Compose](https://developer.android.com/develop/ui/compose/setup-compose-dependencies-and-compiler).

## Организация разработки

[TODO.md](TODO.md) — общий план и источник статусов. [docs/SPEC.md](docs/SPEC.md) — начальные правила экспедиции и открытые решения.

`main` — основная ветка. Для каждой задачи используем `feat/Pxx-short-name` или `fix/short-name`, изменения принимаем через pull request после сборки. Перед параллельной работой согласуем интерфейсы и владельцев общих файлов; для независимых задач используем отдельные worktree. Не коммитим ключи подписи, токены и локальные настройки SDK.
