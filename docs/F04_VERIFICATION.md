# F04 — главный экран активной экспедиции

Проверено 15.09.2026 на Pixel 7 / Android 16 эмуляторе. Книжная сцена Лондона взята из `docs/design/p03/exports/book-412x915.png` и хранится отдельным drawable. Надписи, маршрут и показатели — Compose-элементы из `Expedition`, не часть иллюстрации. Сцена статична; системное уменьшение анимаций не меняет доступность информации.

## Контракт показателей

- «Сегодня» — дата `Instant.now()` в сохранённом `expedition.zone`. В первый день F02 считывает окно от `startedAt`, далее от локальной полуночи до текущего момента. Число отображается только при наличии ключа в `dailySteps`; отсутствующий итог помечен «Нет данных», подтверждённый ноль останется нулём.
- Номер дня приходит из `dayNumber(now)`. Прогресс Лондон → Суэц — известные шаги `firstLegSteps` из `firstLegGoal`; пустая карта не рисует нулевой прогресс. Следующая остановка и остаток берутся из `nextStop` и масштабированных порогов модели. При исправлении итогов назад остановка может снова стать следующей, открытый дневник сохраняется.
- При отказе, ошибке, отсутствии или обновлении Health Connect сохранённый итог подписан как последний известный. Частичная сверка может пропустить именно сегодняшний день: API сообщает число пробелов, но не их даты, поэтому показанный сохранённый итог может быть устаревшим. Экран предупреждает о пробелах и называет путь известным.

## Проверки

- `:app:assembleDebug`, `:app:assembleDebugAndroidTest`, `:app:testDebugUnitTest`, `:app:lintDebug` — успешно, Gradle offline.
- Эмулятор: пустая F03-настройка с режимами и темпами открылась после очистки тестовых данных; активная экспедиция с отсутствующим сегодняшним итогом показала «Нет данных» и неизвестный прогресс. При размере 360×640 dp и шрифте 1.5× вкладки, дневник и диагностика доступны, содержимое прокручивается. Скриншоты: [`f04-home-emulator.png`](screenshots/f04-home-emulator.png), [`f04-compact-large-emulator.png`](screenshots/f04-compact-large-emulator.png), [`f04-diary-emulator.png`](screenshots/f04-diary-emulator.png), [`f04-health-emulator.png`](screenshots/f04-health-emulator.png), [`f04-onboarding-emulator.png`](screenshots/f04-onboarding-emulator.png).
- После исправления проверки `Build.PRODUCT` существующая `SyntheticProgressSeedTest` выполнилась на этом эмуляторе (`OK (1 test)`), сохранила один день с **искусственными** 4 500 шагами. Новый `JourneyHomeUiTest` через дерево доступности подтвердил название участка, 4 500 шагов и Кале (`OK (1 test)`). UI показал 4 500 из 49 000, Кале как следующую остановку и 2 500 до неё: [`f04-synthetic-emulator.png`](screenshots/f04-synthetic-emulator.png). Сразу после теста доступ к Health Connect не был выдан, поэтому экран правильно пометил число как последний известный итог.
- `git diff --check` — без замечаний.

Это проверка интерфейса и сохранённых синтетических данных на эмуляторе. Реальные шаги, часы, задержки источника и отзыв после числовых данных остаются в аппаратной приёмке P02/F02. Ветки карты и паспорта как отдельные экраны F04 не реализует.

## Дополнение P04 (2026-09-19): атмосферные сцены

Ветка `feat/P04-atmospheric-scenes`. Текст выше описывает состояние до P04; контракт показателей и состав текстовых состояний не изменились.

- Растровая книжная сцена `london_book_scene.png` (источник `docs/design/p03/exports/book-412x915.png`) удалена из `app/src/main/res/drawable/` и заменена процедурными тёмными сценами на Compose Canvas (`JourneyScenes.kt`), спецификация — `docs/design/p04-scenes.md`. Чужих ассетов нет; история Git сохраняет файл.
- Десять сцен по порогам главы 1 привязаны к индексам `expedition.stops` через `FirstLegMapProjection.from(expedition)`, поэтому масштабирование порогов темпом сцен не ломает. Позиция `currentIndex + fractionToNext` анимируется кроссфейдом 750 мс в обе стороны (откат шагов — тот же механизм); на Суэце (`nextIndex == null`) сцена стабильна.
- Фазы суток (ночь/рассвет/день/закат) считаются по `expedition.zone` с фиксированными границами; палитры плавно лерпятся в пределах ±30 минут у границы. Светила детерминированы временем, огни горят в сумерках и ночью.
- Вся страница «Путь» тёмная: локальная палитра внутри `JourneyHome` (фон `#0E1B26`, светлый текст, тёмные карточки). `MaterialTheme`, `MainActivity`, строка вкладок и баннер «Новое событие» не менялись и остаются светлыми.
- Доступность: `contentDescription` вида «Сцена пути: Дувр: белые скалы и маяк над линией моря»; при `fontScale >= 1.3` сцена заменяется текстовым пояснением текущей локации (прецедент карты); при системном `animatorDurationScale == 0` сцена статична, переходы мгновенные (бесконечных анимаций нет в принципе).
- Проверки на 2026-09-19: `:app:assembleDebug`, `:app:testDebugUnitTest` (39 тестов после правок astra-review, включая новый `JourneyScenesTest` — фазы, лерп палитр у границ, выбор сцены/доли, позиция героя), `:app:lintDebug` (0 ошибок, 14 предупреждений, все прежние). Эмулятор Pixel 7 / Android 16: `:app:connectedDebugAndroidTest` с `syntheticSeed=allow` — 11/11 (включая `JourneyHomeUiTest`, обновлён для сбора текста с прокруткой: карточка следующей остановки ушла за fold); `SyntheticProgressSeedTest` отдельно через `am instrument` (синтетические 4 500 шагов). Astra-review: «принять с правками»; major M1 (герой в стабильной сцене) и minor m1/m2/m5 исправлены, m3/m4/m6 зафиксированы как допустимые.
- Скриншоты P04 (Pixel 7 / Android 16, синтетические 4 500 шагов, фаза «закат» ~21:05 MSK): [главный экран](screenshots/p04-home-dusk.png), [360×640 dp](screenshots/p04-home-compact.png), [крупный шрифт 1.5 — текстовый fallback](screenshots/p04-home-large-font.png), [reduced-motion](screenshots/p04-home-reduced-motion.png).
- Визуальные ограничения, замеченные штабом на скриншоте Дувр→Кале (fraction ≈ 0.17): полупрозрачный прямоугольник в небе слева от маяка (похоже на оторванный луч/блок входящей сцены) и удвоенные перила, уходящие ниже линии земли. Эстетика силуэтов требует художественной итерации; функциональным контрактам это не противоречит.

## Дополнение P05 (2026-09-19): нижняя навигация

Ветка `feat/P05-modern-navigation`. Верхние вкладки 2×2 TextButton заменены закреплённой внизу Material 3 `NavigationBar` с собственными линейными иконками и бейджем непрочитанных на «Дневнике»; корневой скролл перенесён внутрь состояний, контент прокручивается над панелью. Спецификация — `docs/design/p05-navigation.md`. Метки разделов остались текстом, контракт UI-тестов сохранён; скриншоты P04 выше показывают старую навигацию. Проверки: 39 unit-тестов, lint (0 ошибок), 11/11 instrumentation на Pixel 7 / Android 16; astra-review «принять с правками», m1/m2 исправлены, m3 подтверждён скриншотом fontScale 1.5.

- Скриншоты P05 (Pixel 7 / Android 16, синтетические 4 500 шагов): [«Путь» с панелью и бейджем](screenshots/p05-journey-nav.png), [«Карта»](screenshots/p05-map-nav.png), [«Дневник»](screenshots/p05-diary-nav.png), [360×640 dp](screenshots/p05-compact.png), [крупный шрифт 1.5](screenshots/p05-large-font.png).

## Дополнение P06 (2026-09-20): новый облик сцен

Ветка `feat/P06-fantasy-hike-redesign` переработала сцены по заданию владельца (`docs/design/KIMI_FANTASY_HIKE_REDESIGN_PROMPT.md`): линия земли поднята до 0.68 высоты сцены (большое небо), добавлена дымка у горизонта, герой заменён на цельный силуэт путника XIX века (шляпа, пальто, сумка, посох), крыши получили разнообразные силуэты (фронтон/мансарда/парапет, трубы, шпили), горы — зубчатые хребты со снежными шапками, дым — мягкий шлейф. Замеченные ранее баги исправлены: «прямоугольник в небе у маяка» (жёсткий луч и бледная плита скал ночью — луч стал мягким конусом с затуханием, скалы и снег темнеют через `starAlpha`) и «удвоенные перила ниже земли» (жёсткая кромка полосы моря — море и канал теперь растворяются градиентом). Эйфелева башня удалена из сцены Парижа как анахронизм (действие 1872 года). Контракты чистых функций (`dayPhaseAt`, `paletteFor`, `sceneBlendAt`, `heroFractionAt`, `celestialPosition`, `JOURNEY_SCENES`) не менялись; текстовый fallback сцены при fontScale ≥ 1.3 убран по требованию P06 — сцена остаётся, при переполнении работает прокрутка. Проверки и скриншоты: `docs/design/p06-redesign.md`, `docs/screenshots/p06-*`.
